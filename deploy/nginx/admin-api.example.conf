# Admin / manager API (behind admin UI or dedicated host)
# admin.atris.site UI usually proxies /auth /admin /media to this gateway (:8080)
#
# If you expose admin API separately:
#   proxy_pass http://127.0.0.1:8080;

# Keep existing admin.*.conf pointing admin-ui (:8088).
# api-gateway (admin profile) listens on host :8080.
